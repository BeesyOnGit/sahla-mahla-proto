import React, { useEffect } from "react";

function PageTopAlling({ children }: any) {
    // const App = document.getElementsByClassName("App")[0];
    window.scrollTo(0, 0);

    return children;
}

export default PageTopAlling;
